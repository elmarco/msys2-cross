--- a/PKGBUILD
+++ b/PKGBUILD
@@ -80,11 +80,11 @@
   CFLAGS+=" -DHAVE_BZIP2"
   ../${_realname}-${pkgver}/contrib/minizip/configure \
     --prefix=${MINGW_PREFIX} \
-    --build=${MINGW_CHOST} \
     --host=${MINGW_CHOST} \
     --target=${MINGW_CHOST} \
     --enable-demos \
-    LIBS="-lbz2"
+    LDFLAGS="-L${srcdir}/build-${MSYSTEM}" \
+    LIBS="-lbz2 -lz"

   make -j1
 }
